spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://localhost:3306/elearning_management
    username: root
  #    password:
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      ddl-auto: none
    show-sql: true

  application:
    name: product-service

server:
  port: 8085
