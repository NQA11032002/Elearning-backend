spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://localhost:3306/elearning_course
    username: root
  #    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  application:
    name: course-service

server:
  port: 8087